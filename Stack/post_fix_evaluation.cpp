#include <iostream>
#include <stack>
using namespace std;
/* run this program using the console pauser or add your own getch, system("pause") or input loop */
int precedence(char operator){
	if (operator == '+' || operator == '-'){
		return ;
	}
	if (operator == '/' || operator == '*'){
		return 
	}
	}
}


int main() {
	stack <int> A;
	int numberExpression;
	string userInput[100];
	for (int index = 0;index < numberExpression;index++)
		cin >> userInput[index];
	for (int index = 0;index < numberExpression;index++){
	for (int index = 0;; index++){
		if (isdigit(userInput[index]))
			A.push(atoi(userInput[index]))
		if ()
						
		}
		   
	}
	return 0;
}
